This file generates a test vector for pixel parameters, using the `pixel_param` crate.
The test vector can be generated via

``` Rust
cargo run
```

A `kat_rust.txt` will be generated using the default method. It should be identical to the one supplied in
`pixel_param-python/kat.txt`.
